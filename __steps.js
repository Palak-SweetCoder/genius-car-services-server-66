/**
 * -------------------------------------------------------------------------------
 * -----THIS FILE IS CREATED TO SHOW THE EXTRA STEPS FROM OUR PREVIOUS STEPS------
 * -------------------------------------------------------------------------------
 * 
 * -------------------------------
 * ------BASIC SERVER SETUP-------
 * -------------------------------
 * Go to expressjs.com >> getting started >> installing
 * CMD go to projects >> run this command >> mkdir appname
 * Go to app folder run this command >> npm init or npm init -y (-y for avoid multiple questions)
 * (EXTRA)Run this command >> npm i express cors mongodb dotenv (dotenv for make server user and pass more secure)
 * Create index.js file
 * (EXTRA)Create a .env file in the root of our project
 * Require express
 * Require cors
 * (EXTRA)Require the dotenv (require('dotenv').config();) follw this url (https://www.npmjs.com/package/dotenv)
 * Declare port
 * Declare app
 * Use middlewares
 * Server side setup COMPLETED!!!
 * 
 * -----------------------------------------
 * -----CONNECT SERVER WITH THE MONGODB-----
 * -----------------------------------------
 * Go to mongodb atlas >> connect: copy the code and paste it to the server side for working
 * Go to mongodb atlas >> database >> create database >> insert document video(66.3)
 * Go to database access >> add new database user
 * Go to .env file and set the user and password
 * Make uri template string and dynamically set user and password
 * Console log connection status into the client.connect function under collection
 * If console log successfylly to the cmd then connection done...
 * 
 */